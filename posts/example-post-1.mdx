---
type: Post
title: Osnove HTML-ja
description: >-
  Dobrodošli na strani, kjer se boste naučili osnov jezika HTML! Spoznali boste,
  kako ustvariti osnovno strukturo spletne strani, uporabljati oznake (npr.
  <html>, <head>, <body>) in dodati osnovne elemente, kot so naslovi, odstavki,
  povezave in slike. Idealen vodnik za začetnike, ki želijo začeti svojo pot v
  spletnem razvoju.
date: '2024-11-06'
---
# Uvod v HTML

HTML (HyperText Markup Language) je osnovni gradnik spleta, ki omogoča ustvarjanje strukturiranih dokumentov in spletnih strani. S pomočjo HTML-ja definiramo vsebino in njeno osnovno hierarhijo, ki jo brskalniki interpretirajo in prikazujejo uporabnikom.

## Osnovna struktura HTML-dokumenta

Vsak HTML-dokument se začne s strukturo, ki vključuje glavne oznake: `<html>`, `<head>`, in `<body>`.

### Primer osnovne strukture:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Moja prva spletna stran</title>
  </head>
  <body>
    <h1>Dobrodošli!</h1>
    <p>To je moja prva HTML-stran.</p>
  </body>
</html>
```

## Naslovi

Naslovi so ključni za organizacijo vsebine. HTML podpira šest stopenj naslovov, od `<h1>` do `<h6>`, kjer `<h1>` označuje najpomembnejši naslov.

### Primeri naslovov:

*   `<h1>` Glavni naslov

*   `<h2>` Podnaslov

*   `<h3>` Manjši podnaslov

*   `<h4>` Podpodnaslov

*   `<h5>` Skoraj najmanjši podnaslov

*   `<h6>` Najmanj pomemben naslov

## Poudarki

Uporabite različne oznake za poudarjanje besedila:

*   **Krepko besedilo**: `<strong>` ali `<b>`

*   *Ležeče besedilo*: `<em>` ali `<i>`

*   Prečrtano besedilo: `<del>`

## Seznami

HTML omogoča urejene in neurejene sezname.

### Neurejen seznam:

```
<ul>
  <li>Prva točka</li>
  <li>Druga točka</li>
  <li>Tretja točka</li>
</ul>
```

### Urejen seznam:

```
<ol>
  <li>Prva točka</li>
  <li>Druga točka</li>
  <li>Tretja točka</li>
</ol>
```

## Koda

V HTML-ju lahko prikažemo kodo z oznako `<code>` za enovrstično kodo ali `<pre>` za blok kode.

### Primer:

**Inline koda**: `<div>To je div element.</div>`

**Blok kode**:

```
<pre>
  <code>
    <h1>Naslov</h1>
    <p>To je odstavek.</p>
  </code>
</pre>
```

```
Tabele
```

Tabele organizirajo podatke v vrsticah in stolpcih.

### Primer tabele:

```
<table>
  <tr>
    <th>Ime</th>
    <th>Starost</th>
  </tr>
  <tr>
    <td>Janez</td>
    <td>25</td>
  </tr>
  <tr>
    <td>Marija</td>
    <td>30</td>
  </tr>
</table>
```

## Povezave

Povezave v HTML-ju so ustvarjene z oznako `<a>`.

Primer:

```
<a href="https://www.example.com">
``````
</a>
```

## Slike

Slike v HTML-ju dodamo z oznako `<img>`.

Primer:

```
<img src="slika.jpg" alt="Opis slike">
```

***

